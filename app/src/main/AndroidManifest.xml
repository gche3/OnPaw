<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions for OpenStreetMap -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnPaw">

        <!-- Launcher / Home screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.OnPaw">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PetProfilesActivity"
            android:exported="true"
            android:theme="@style/Theme.OnPaw">
        </activity>
        <activity
            android:name=".IndividualPetActivity"
            android:exported="true"
            android:theme="@style/Theme.OnPaw">
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.OnPaw">
        </activity>
        <activity
            android:name=".FindSittersActivity"
            android:exported="true"
            android:theme="@style/Theme.OnPaw">
        </activity>
        <activity
            android:name=".LoadingActivity"
            android:exported="true"
            android:theme="@style/Theme.OnPaw">
        </activity>

        <!-- Normal Find Pet Care screen -->
        <activity
            android:name=".FindPetCareActivity"
            android:exported="false"
            android:theme="@style/Theme.OnPaw" />

        <!-- Emergency Find Pet Care screen -->
        <activity
            android:name=".EmergencyFindPetCareActivity"
            android:exported="false"
            android:label="@string/title_activity_emergency_find_pet_care"
            android:theme="@style/Theme.OnPaw" />

        <!-- Chat screen -->
        <activity
            android:name=".ChatActivity"
            android:exported="false" />

    </application>

</manifest>
